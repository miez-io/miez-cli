# SRS: <capability>

ISO/IEC/IEEE 29148 software requirements specification.

## 1. Introduction

Purpose and why this capability exists.

## 2. Stakeholders and scope

- Stakeholders:
- Actors:
- In scope:
- Out of scope: see Non-goals

## 3. Non-goals

- …

## 4. Requirements

### 4.1 Functional

#### <capability>/<short-name>

When `<event>`, the system shall `<behavior>`.

Acceptance:

- WHEN `<event>` THEN `<observable result>`

### 4.2 Quality

#### <capability>/<short-name>

While `<state>`, the system shall `<behavior>`.

Acceptance:

- WHEN `<condition>` THEN `<metric, unit, threshold, scope>`

### 4.3 Constraints

#### <capability>/<short-name>

The system shall `<behavior>`.

Acceptance:

- WHEN `<condition>` THEN `<observable result>`

### 4.4 Interface

Write "not applicable" when there is no API or UI.

#### <capability>/<short-name>

When `<event>`, the system shall `<behavior>`.

Acceptance:

- WHEN `<event>` THEN `<visible or protocol state>`

## 5. Open questions

- …
